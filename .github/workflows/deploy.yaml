name: Deploy MultiDocker
on:
  push:
    branches:
      - main # check your repo, your default branch might be master!

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: docker login -u ${{ secrets.DOCKER_USERNAME }} -p ${{ secrets.DOCKER_PASSWORD }}
      - run: docker build -t mykola123danylko/react-test -f ./client/Dockerfile.dev ./client
      - run: docker run -e CI=true mykola123danylko/react-test npm test

      - run: docker build -t mykola123danylko/multi-client ./client
      - run: docker build -t mykola123danylko/multi-nginx ./nginx
      - run: docker build -t mykola123danylko/multi-server ./server
      - run: docker build -t mykola123danylko/multi-worker ./worker

      - run: docker push mykola123danylko/multi-client
      - run: docker push mykola123danylko/multi-nginx
      - run: docker push mykola123danylko/multi-server
      - run: docker push mykola123danylko/multi-worker